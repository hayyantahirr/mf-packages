export const metadata = {
  title: "Secure Checkout | MF Packages",
  description: "Complete your order with MF Packages. Secure payments, fast checkout, and global shipping options for your packaging solutions.",
};

export default function CheckoutLayout({ children }) {
  return children;
}
